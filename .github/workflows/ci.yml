name: CI

# CI desabilitado temporariamente: o deploy real e feito pelo EasyPanel,
# que clona o repo e roda Dockerfile na raiz. CI ficou so disponivel via
# trigger manual em Actions > CI > Run workflow.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    name: CI desabilitado (use EasyPanel para deploy)
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo "CI manual placeholder. EasyPanel handles deploys."
